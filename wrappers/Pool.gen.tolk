// Auto-generated wrapper for contract 'Pool'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/messages"
import "@contracts/types"

/// Storage `PoolStorageNotInitialized` is defined in `@contracts/types`
struct Pool {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun Pool.fromStorage(
    storage: PoolStorageNotInitialized,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Pool {
    val stateInit = ContractState {
        code: build("Pool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Pool { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun Pool.fromAddress(address: address): Pool {
    return Pool { address };
}

/// Deploys the contract to the blockchain
fun Pool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendTextComment(
    self,
    from: address,
    comment: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TextComment { comment },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendUpdateVset(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: UpdateVset { queryId },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendNewStake(
    self,
    from: address,
    queryId: uint64,
    value: coins,
    signedBody: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NewStake { queryId, value, signedBody },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendNewStakeOk(
    self,
    from: address,
    queryId: uint64,
    answer: uint32,
    remaining: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NewStakeOk { queryId, answer, remaining },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendNewStakeError(
    self,
    from: address,
    queryId: uint64,
    reason: uint32,
    remaining: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NewStakeError { queryId, reason, remaining },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendRecoverStakeCompat(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RecoverStakeCompat { queryId },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendRecoverStakeUnrestricted(
    self,
    from: address,
    queryId: uint64,
    validator: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RecoverStakeUnrestricted { queryId, validator, amount },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendRecoverStakeOk(
    self,
    from: address,
    queryId: uint64,
    remaining: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RecoverStakeOk { queryId, remaining },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendRecoverStakeError(
    self,
    from: address,
    queryId: uint64,
    answer: uint32,
    remaining: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RecoverStakeError { queryId, answer, remaining },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendPayoutBurnNotification(
    self,
    from: address,
    queryId: uint64,
    isWithdrawal: bool,
    owner: address,
    index: uint64,
    roundIndex: uint64,
    distribution: Distribution,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PayoutBurnNotification {
            queryId,
            isWithdrawal,
            owner,
            index,
            roundIndex,
            distribution,
        },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendAddFunds(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AddFunds { queryId },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendAddValidator(
    self,
    from: address,
    queryId: uint64,
    validator: address,
    roundAllowance: RoundAllowance,
    limits: ValidatorLimitTon | ValidatorLimitShare | null,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AddValidator { queryId, validator, roundAllowance, limits },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendUpdateLimits(
    self,
    from: address,
    queryId: uint64,
    limit: GlobalValidatorsLimit | GlobalNominatorsLimit | ValidatorSpecific,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: UpdateLimits { queryId, limit },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendRemoveValidator(
    self,
    from: address,
    queryId: uint64,
    validator: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RemoveValidator { queryId, validator },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendOwnerWithdrawal(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: OwnerWithdrawal { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun Pool.sendInitPoolMessage(
    self,
    from: address,
    queryId: uint64,
    mainValidator: address,
    roundAllowance: RoundAllowance,
    limit: ValidatorLimitTon | ValidatorLimitShare | null,
    ownerShare: uint25,
    maxTonPerValidator: coins,
    minTonPerValidator: coins,
    maxRefund: coins,
    nominatorsSettings: NominatorsSettings,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: InitPoolMessage {
            queryId,
            mainValidator,
            roundAllowance,
            limit,
            ownerShare,
            maxTonPerValidator,
            minTonPerValidator,
            maxRefund,
            nominatorsSettings: nominatorsSettings.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Pool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Pool.getMaxPunishment(self, stake: int): int {
    return net.runGetMethod(self.address, "get_max_punishment", [stake]);
}

fun Pool.owner(self): address {
    return net.runGetMethod(self.address, "owner");
}

fun Pool.getPoolData(self): Storage {
    return net.runGetMethod(self.address, "get_pool_data");
}

fun Pool.getProxyAddress(self, validator: address): GetProxyAddressResult {
    return net.runGetMethod(self.address, "get_proxy_address", [validator]);
}

fun Pool.getNominatorData(self, nominatorAddress: int): GetNominatorData {
    return net.runGetMethod(self.address, "get_nominator_data", [nominatorAddress]);
}

fun Pool.getValidatorInfo(self, address: address): GetValidatorInfo {
    return net.runGetMethod(self.address, "get_validator_info", [address]);
}

fun Pool.getValidatorInfoMtc(self, workchain: int, hash: int): GetValidatorInfo {
    return net.runGetMethod(self.address, "get_validator_info_mtc", [workchain, hash]);
}

fun Pool.getLimitsPerValidator(self): (coins, coins, coins) {
    return net.runGetMethod(self.address, "get_limits_per_validator");
}

fun Pool.getNominatorMinimalStake(self): GetMinStake {
    return net.runGetMethod(self.address, "get_nominator_minimal_stake");
}
